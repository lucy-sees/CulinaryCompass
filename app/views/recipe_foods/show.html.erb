  <%= render @recipe_food %>

  <div class="mt-4">
    <%= link_to "Edit this recipe food", edit_recipe_food_path(@recipe_food), class: 'btn btn-primary mr-2' %> |
    <%= link_to "Back to recipe foods", recipe_foods_path, class: 'btn btn-secondary mr-2' %>

    <%= button_to "Destroy this recipe food", @recipe_food, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
  </div>
