  <%= render @recipe_food %>

  <div class="mt-4 d-flex gap-2">
    <%= link_to "Edit this recipe food", edit_recipe_food_path(@recipe_food), class: 'btn btn-primary' %> |
    <%= link_to "Back to recipe foods", recipe_foods_path, class: 'btn btn-secondary' %> |

    <%= button_to "Delete this recipe food", @recipe_food, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
  </div>
