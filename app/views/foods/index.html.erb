  <h1 class="my-4">Foods</h1>

  <div id="foods">
    <% @foods.each do |food| %>
      <%= render food %>
      <p>
        <%= link_to "Show this food", food, class: 'btn btn-info mb-2' %>
      </p>
    <% end %>
  </div>

  <%= link_to "New food", new_food_path, class: 'btn btn-primary' %>
