  <div id="users" class="mt-2">
    <% @users.each do |user| %>
      <%= render user %>
      <p>
        <%= link_to "Show this user", user, class: 'btn btn-info mb-2' %>
      </p>
    <% end %>
  </div>

  <%= link_to "New user", new_user_path, class: 'btn btn-primary' %>
